#include<iostream>
using namespace std;
int main(){ 
    
    /* a^a = 0
       a^0 = a

       binary search is applicable on only monotonic funtion
       (means increasing or decreasing)

       total no of occuranse formula = (last occ- first occ + 1 )

       upper case to lower case formula = char -'A' + 'a' 
       lower case to upper case formula = char -'a' + 'A' 
       char to number formula = char - '0'

       in 2D maatrix for binarysearch (start = 0) (end = row*col - 1)

       formula to find GCD of no is GCD = (a-b , b)

      */

    return 0; 
}